services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: menuos
      POSTGRES_PASSWORD: menuos
      POSTGRES_DB: menuos
    ports:
      - "5433:5432"
    volumes:
      - menuos_pg_data:/var/lib/postgresql/data

volumes:
  menuos_pg_data:
