import { Card } from "@/components/ui/card";

export default function MenusPage() {
  return (
    <Card>
      <h1 className="font-serif text-xl font-bold text-primary">Menus</h1>
      <p className="mt-2 text-sm text-slate-600">
        Menu CRUD coming in next sprint. Schema and dashboard shell are ready.
      </p>
    </Card>
  );
}
